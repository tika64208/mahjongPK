[project]
name = "longyan-mahjong"
version = "0.1.0"
description = "A local Longyan Mahjong engine with one human player and three bots."
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]

